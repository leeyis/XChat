# 任务计划：XChat React 全量重构

## 目标
以 `ui-ref/xchat-desktop-prototype.html` 和 `ui-ref/DESIGN.md` 为唯一视觉基准，彻底替换旧前端，并让聊天、主机、文件、设置以及群聊、已读回执、传输取消、截图快捷键通过真实 React + Rust 能力运行。

## 当前阶段
阶段 18：4/8/16 最大并行通道完整实现

## 各阶段

### 阶段 1：需求与发现
- [x] 阅读最新设计、原型、素材与现有工程说明
- [x] 审计现有前端运行方式和 Tauri/Web 两条后端路径
- [x] 识别可复用能力与必须补充的数据库、协议和平台能力
- **状态：** complete

### 阶段 2：设计固化与实施规划
- [x] 比较三种 React—Rust interface 方案
- [x] 用户批准深 `XChatModule` 方案
- [x] 写入、提交并自审设计文档
- [x] 用户复核已提交的设计文档
- [x] 形成可执行的分阶段实现计划
- **状态：** complete

### 阶段 3：React 前端与运行链路
- [x] 建立最小 React/Vite 工程并替换旧界面
- [x] 实现 `XChatModule`、Tauri adapter 和 HTTP/WebSocket adapter
- [x] 实现聊天、主机、文件、设置四个设计模块
- [x] 构建产物保持 Tauri 与 RustEmbed 可用
- **状态：** complete

### 阶段 4：共享 Rust 能力
- [x] 增加兼容迁移和共享数据模型
- [x] 实现群聊、已读、文件中心、传输取消和设备元数据
- [x] 同时接入 Tauri commands 与 Axum HTTP/WebSocket
- [x] 实现 macOS/Web 截图路径及能力声明
- **状态：** complete

### 阶段 5：测试与视觉验证
- [x] 运行前端构建和最小逻辑检查
- [x] 运行 Rust 测试及 desktop/web 双 feature 编译
- [x] 使用隔离端口和数据库冒烟测试
- [x] 对照最新原型检查主要布局、状态与交互
- **状态：** complete

### 阶段 6：交付
- [x] 检查 diff，不覆盖用户既有改动
- [x] 记录运行、预览和验证命令
- [x] 汇报实现结果与平台限制
- **状态：** complete

### 阶段 7：0.1.0 稳定性与品牌修复
- [x] 品牌、版本、bundle identifier 和应用数据目录统一为 Xchat 0.1.0
- [x] 重新生成透明无白边的全平台图标
- [x] 修复设备 MAC/内存周期交替
- [x] 修复 macOS 隐藏窗口后 Dock 重开
- **状态：** complete

### 阶段 8：React 基础交互与文件中心
- [x] 恢复表情面板和光标插入
- [x] 全部空状态在可用区域垂直居中
- [x] 设置导航增加选中态、滚动同步和移动端行为
- [x] 会话信息默认收起
- [x] 文件中心严格对齐最新原型并处理本地文件不可用
- **状态：** complete

### 阶段 9：截图编辑与附件草稿
- [x] 实现单例置顶截图编辑窗口
- [x] 实现矩形、椭圆、箭头、画笔、马赛克、文本和回退
- [x] 实现完成、取消和单例钉图
- [x] 截图、粘贴图片和选择文件先进入会话附件草稿
- [x] 一次发送文字和多个独立附件，失败项保留
- **状态：** complete

### 阶段 10：安全媒体读取与图片内联
- [x] 将编辑截图保存到受管持久 outbox
- [x] 新增按 message_id 校验的桌面媒体读取
- [x] 收发双方完成后直接渲染图片
- [x] 缺失文件回退文件卡片且不再弹路径错误
- **状态：** complete

### 阶段 11：集成验证与交付
- [x] React build 与最小逻辑检查
- [x] Rust tests 与 desktop/web 双 feature 编译
- [x] 隔离数据库运行 Tauri 桌面烟测
- [x] 对照书面规格和原型完成视觉/交互验收
- [x] 检查 diff、提交实现并给出运行/构建命令
- **状态：** complete

### 阶段 12：测试反馈二次收敛
- [x] 对真实运行实例建立 MAC/内存周期变化的红灯复现
- [x] 研究微信截图悬浮工具栏并确认本轮交互设计
- [x] 写入、自审并单独提交第二轮设计规格
- [x] 用户复核已提交的书面规格
- [x] 写入可执行实施计划并划分并行文件边界
- [x] 修复同一设备多来源元数据覆盖及默认机器名
- [x] 扩展表情集合并在选择后自动关闭
- [x] 重做截图区域下方悬浮编辑工具栏
- [x] 支持 Tauri/Web 外部文件拖入输入区形成附件草稿
- [x] 文件卡显示真实进度、百分比和速度，并实现兼容的 4 路并行传输
- [x] 文件“打开”提供打开文件与在文件夹中显示
- [x] 设置身份页显示稳定的本机 IP 与 MAC
- [x] 运行回归、双实例烟测、构建并提交
- [x] 修复并行传输 v2 的能力降级、失败回滚、远端失败状态与 finalize 幂等性
- **状态：** complete

### 阶段 13：截图钉图、通知与文本二次编辑
- [x] 确认用户批准的完整范围与书面设计
- [x] 建立钉图、文本编辑/移动和设置高亮的红色回归
- [x] 完整合入 `9c27425` 的钉图、通知、托盘和设备上线旁枝
- [x] 实现文本二次编辑、拖动定位及原子撤销/重做
- [x] 移除设置导航粉红色选中背景
- [x] 运行前端、Rust、生产构建与桌面烟测
- [x] 检查最终 diff 并保留用户暂存/未跟踪内容
- **状态：** complete

### 阶段 14：截图交互反馈修复
- [x] 用浏览器真实指针回放复现马赛克松手后消失
- [x] 建立马赛克预览与文本删除红灯回归
- [x] 修复非文本标注被预览过滤的问题
- [x] 支持编辑态文本拖动和清空删除，并保留撤销/重做
- [x] 移除独立钉图小工具条，复用截图编辑器继续标注
- [x] 完成浏览器交互、前端测试、生产构建与 Rust 双 feature 验证
- **状态：** complete

### 阶段 15：网络发现、Presence 与消息可靠性工程评审
- [x] 完整阅读 2026-08-18 设计文档和最新桌面原型
- [x] 确认 UI 原型已经确认、工程方案仍需结合当前代码评审
- [x] 使用代码知识图核对发现、在线状态、补发与前端刷新调用链
- [x] 将阶段 A 收敛为独立、可回滚、可验证的首个实施切片
- [x] 获得工程切片确认并确定在当前分支依次实施 A0、A1
- [x] A0：以失败测试锁定接口分类、定向广播、发送预算与回复去重
- [x] A0：替换 256 网段扫描和 2 秒心跳，接入启动突发、稳态周期、固定地址退避与诊断日志
- [x] A1：持久化发现设置并统一接入 Tauri/Web 设置接口
- [x] A1：按已批准原型实现接口管理、风险确认、恢复推荐与刷新交互
- [x] 完成前端、Rust 双 feature、真实运行和视觉烟测
- **状态：** complete

### 阶段 16：Windows 接口隔离与发现关闭语义收敛
- [x] 解释 75 秒离线门控与旧版 128 台兼容桥的实际含义
- [x] 用户确认 Windows 纳入本轮、Android 暂缓、无需旧版协议兼容
- [x] 核对当前 UUID、动态 IP、接收校验与旧地址错投窗口
- [x] 提交 Windows ingress/reply、离线硬门禁和兼容桥删除的短设计并获得确认
- [x] 在原型中明确关闭发现的用户后果，并设计手工地址测试/身份确认交互
- [x] 获得设备身份、离线文案和“测试连接”原型确认
- [x] 以失败测试锁定跨平台接口选择和已知地址发送语义
- [x] 实现 Windows packet-info 收发、移除旧版心跳桥并保持离线发送硬门禁
- [x] 实现手工地址测试、设备身份展示与安全保存链路
- [x] 完成 macOS 回归、Windows target 编译及可用范围内的运行验证
- **状态：** complete

### 阶段 18：4/8/16 最大并行通道完整实现
- [x] 核对原型、设计文档、生产设置页和 Git 历史，确认功能只存在于已批准原型
- [x] 审计并行 v2 的设置、协议、调度、重试和恢复调用链，固化共享契约
- [x] 写入并自审可执行实施计划
- [x] 以失败测试锁定默认值、合法值、持久化和 Tauri/Web 设置兼容
- [x] 以失败测试锁定新传输的 4/8/16 分块选择、旧端兼容和全局并发上限
- [ ] 按已批准原型实现生产设置项及中英文文案
- [ ] 完成前端、Rust、desktop/web 构建、隔离运行、视觉与文件实传验证
- [ ] 审查差异、提交实现并交付
- **状态：** in_progress

## 已做决策
| 决策 | 理由 |
|------|------|
| 采用深 `XChatModule`：`getSnapshot/dispatch/subscribe` | React 只学习一个 interface，Tauri/Web 差异和跨模块状态机保持 locality |
| React 源码放入 `frontend/`，构建到现有 `src/` | 彻底替换旧 UI，同时避免破坏 Tauri `frontendDist` 与 RustEmbed 的静态目录约定 |
| UUID 保持设备主身份，MAC/hostname 为可选元数据 | 兼容历史消息、SQLite 数据和现有局域网协议 |
| 群聊由 Rust 扇出，送达/已读按稳定消息 ID 记录每成员 ack | 避免前端伪群聊、设备时钟偏差和跨设备本地行 ID 不一致 |
| 不提供生产 mock adapter | 用户要求缺失能力真实实现；不可用平台通过 capability 明示 |
| 不引入 Redux、Router、UI 框架或事件存储 | 两个真实 transport adapter 已足够，额外基础设施没有本轮收益 |
| 截图编辑使用原生 Canvas 操作序列 | 七种首版工具无需新增图形依赖，回退可通过移除最后一次操作后重绘实现 |
| 一次发送仍落为相邻独立消息 | 满足一次编辑/一次发送，同时避免数据库和 wire protocol 迁移 |
| 编辑截图持久化到受管 outbox | 发送完成后仍可在发送方历史内联显示，避免临时截图路径失效 |
| Web 上传持久化到下载目录内的受管 `.xchat-outbox` | 刷新后发送方仍可按消息 ID 预览，同时不开放任意发送源文件 |
| 本机设备元数据用 OnceLock 固定 | 同一进程广播与 reply 必须使用同一 MAC，标准库即可完成 |
| 单文件并行采用能力协商的 v2 分块协议 | 用户确认大文件拆成 4 个等长范围并行；旧客户端继续走 v1 顺序分块 |
| 可调并行采用显式 v3 协议，v2 永远固定四范围 | 防止新客户端向旧接收端发送其 manifest 校验无法接受的 8/16 通道布局 |
| 全局限流器按设置值创建代际 | 同一代际的新传输共享总上限，保存新值不改变已经开始的传输 |
| Finder 拖放使用 Tauri Webview drag-drop 事件 | DOM `dataTransfer.files` 不能可靠提供桌面绝对路径 |
| 钉图“显示工具条”重新进入同一截图编辑器 | 当前钉图只保存扁平 PNG；复用现有编辑器比在钉图视图复制一套 Canvas/history 更小且行为一致 |

## 遇到的错误
| 错误 | 尝试次数 | 解决方案 |
|------|---------|---------|
| `cargo tauri dev -- --port ...` 把应用参数误传给 `cargo run` | 1 | 当前 Tauri CLI 使用 `cargo tauri dev -- -- --port ...` |
| `rtk rg` 使用不存在的 shell glob 导致 zsh `no matches found` | 1 | 对已知配置文件使用显式路径，不再传未解析 glob |
| `imagegen` 去背版本轻微重绘了大部分蓝色像素 | 1 | 丢弃生成结果，改用原图边缘连通近白背景去除并验证 alpha |
| 删除旧截图兼容入口时补丁中的 mobile capability 上下文不匹配 | 1 | 读取实际尾部结构后用精确上下文重新应用 |
| Chrome 扩展不允许浏览器 QA 直接选择本地图片 | 1 | 不改用户扩展权限，改用浏览器会话剪贴板粘贴同一 PNG，成功验证图文草稿 |
| Computer Use 无法定位未打包的 Tauri debug binary | 2 | 保留 Tauri 启动/服务日志烟测，以浏览器真实双实例完成交互验证 |
| 文件规划技能的 `scripts/catchup.py` 不存在 | 1 | 已手工读取三份计划文件并继续记录，不阻塞产品实现 |
| 恢复计划 stash 时被用户未跟踪文件阻止 | 1 | 用户文件始终保留；根据已记录补丁重建计划改动并恢复原始暂存哈希 |
| Tauri 调试实例被已运行的 `/Applications/Xchat.app` 单实例锁接管 | 1 | 不终止用户进程；保留编译、浏览器真实交互与启动到二进制阶段的证据 |
| `browse eval/js` 对异步复现脚本返回空输出 | 2 | 改用分步浏览器哈希对照确认症状，并用 Node 红灯测试固化模型边界 |
| 组合代码图查询用 `text` 作为局部变量覆盖输出 helper | 1 | 记录后改用 `text_message` 命名重新执行，只读查询未产生代码或数据变更 |
| Web mock 的 `capture.pin` 只打开新窗口，不更新 pending | 1 | 仅把它视为浏览器 mock 限制；真实 Tauri 原位替换由 Rust 状态测试和 desktop 编译覆盖 |
| 追加阶段 15 时补丁对 `progress.md` 的模板上下文不匹配 | 1 | 读取文件实际开头后拆分为精确补丁，不重复错误上下文 |

## 备注
- `src-tauri/src/commands.rs` 已有用户修改，实施时只做局部补丁并保留其变更。
- `ui-ref/`、`AGENTS.md`、`plan/UI-DESIGN-PC.md` 等未跟踪内容属于用户，不纳入清理。
- 实施顺序：① React 壳和现有能力回接；② SQLite 兼容迁移；③ 群聊与回执；④ 文件中心与取消；⑤ 设备/截图；⑥ 双运行时与视觉验收。
- 并行文件所有权：前端 agent 只改 `frontend/` 和根 npm/Vite 文件；数据库 agent 只改 `db.rs`；网络 agent 只改 `network/`；主 agent 负责 commands、Web、注册、权限和最终集成。
